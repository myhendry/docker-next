services:
  - type: web
    name: docker-next
    env: docker
    plan: free
    region: singapore
    buildCommand: docker build -t docker-next .
    startCommand: docker run -p $PORT:3000 docker-next
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
